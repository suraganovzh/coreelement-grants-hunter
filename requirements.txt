# Core
python-dotenv==1.0.1
PyYAML==6.0.2

# Web Scraping (used by GitHub Actions)
beautifulsoup4==4.12.3
requests==2.32.3
lxml==5.3.0
httpx==0.28.1
playwright==1.49.1

# Data Processing
pandas==2.2.3
python-dateutil==2.9.0.post0
tqdm==4.66.1

# Database (SQLite — no external DB needed)
SQLAlchemy==2.0.36

# Local LLM (Ollama client — for Surface ARM)
ollama==0.4.7

# Cloud LLM fallbacks (free tiers)
groq==0.15.0
google-generativeai==0.8.4

# Retry Logic
tenacity==9.0.0

# Testing
pytest==8.3.4
pytest-cov==6.0.0
